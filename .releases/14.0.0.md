**Breaking Changes**

* Changed the `PositionSchema` definitions.

**New Features**

* Added an optional parameter to `PositionContainer.getGroup` function which changes the return type.
* Added an optional parameter to `PositionContainer.getGroups` function which changes the return type.
* Added the `snapshot.dividends` attribute to some `PositionSchema` definitions.

**Technical Enhancements**

* Consolidated the `@barchart/portfolio-client-js` and `@barchart/portfolio-api-common` packages into a Lerna monorepo.
* Replaced the Gulp-based build workflow with package scripts and shared build tools.

**Other**

* Synchronized both package versions at `14.0.0`.